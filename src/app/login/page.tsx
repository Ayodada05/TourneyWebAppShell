export default function LoginPage() {
  return (
    <section className="space-y-2">
      <h1 className="text-3xl font-semibold">Login</h1>
      <p className="text-slate-700">
        Authentication will be added after the MVP shell is ready.
      </p>
    </section>
  );
}
